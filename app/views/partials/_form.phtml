<!-- app/views/partials/_form.phtml -->
<form action="<?php echo WEB_ROOT; ?>/alan/add" method="POST" class="bg-white rounded-3xl p-7 w-full shadow-sm border border-gray-100/80 flex flex-col gap-4 my-2">
    
    <!-- Description -->
    <div class="flex flex-col gap-1.5">
        <label class="text-xs font-semibold text-gray-400">Description</label>
        <input type="text" name="description" placeholder="Enter task description..." 
                       class="w-full px-4 py-2 rounded-xl bg-[#f0f4fa] text-sm text-gray-700 placeholder-gray-400 focus:outline-none transition-colors border-none" value="<?php echo htmlspecialchars($this->taskData['description'] ?? ''); ?>">

    </div>

    <!-- Status & Created By -->
    <div class="grid grid-cols-2 gap-4">
        <div class="flex flex-col gap-1.5">
            <label class="text-xs font-semibold text-gray-400">Status</label>
            <select name="status" class="w-full px-4 py-2 rounded-xl bg-[#f0f4fa] text-sm text-gray-700 focus:outline-none transition-colors border-none appearance-none">
            <option value="Pending" <?php echo ($this->taskData['status'] ?? 'Pending') === 'Pending' ? 'selected' : ''; ?>>Pending</option>
            <option value="In Progress" <?php echo ($this->taskData['status'] ?? 'Pending') === 'In Progress' ? 'selected' : ''; ?>>In Progress</option>
            <option value="Completed" <?php echo ($this->taskData['status'] ?? 'Pending') === 'Completed' ? 'selected' : ''; ?>>Completed</option>
            </select>
        </div>

        <div class="flex flex-col gap-1.5">
            <label class="text-xs font-semibold text-gray-400">Created By</label>
                        <input type="text" name="created_by" placeholder="Author name..." value="<?php echo htmlspecialchars($this->taskData['created_by'] ?? ''); ?>"

                   class="w-full px-4 py-2 rounded-xl bg-[#f0f4fa] text-sm text-gray-700 placeholder-gray-400 focus:outline-none transition-colors border-none">
        </div>
    </div>

    <!-- Start Time & End Time -->
    <div class="grid grid-cols-2 gap-4">
        <div class="flex flex-col gap-1.5">
            <label class="text-xs font-semibold text-gray-400">Start Time</label>
	<input type="text" name="start_time" placeholder="--:-- --" value="<?php echo htmlspecialchars($this->taskData['start_time'] ?? ''); ?>"

                   class="w-full px-4 py-2 rounded-xl bg-[#f7fafd] text-sm text-gray-400 focus:outline-none transition-colors border-none">
        </div>

        <div class="flex flex-col gap-1.5">
            <label class="text-xs font-semibold text-gray-400">End Time</label>
	<input type="text" name="end_time" placeholder="--:-- --" value="<?php echo htmlspecialchars($this->taskData['end_time'] ?? ''); ?>"

                   class="w-full px-4 py-2 rounded-xl bg-[#f7fafd] text-sm text-gray-400 focus:outline-none transition-colors border-none">
        </div>
    </div>

    <!-- Botons d'Acció més compactes (py-2 enlloc de py-2.5) -->
    <div class="flex justify-end gap-3 mt-2">
        <button type="submit" class="bg-[#4f46e5] hover:bg-indigo-700 text-white px-7 py-2 rounded-2xl font-bold text-sm transition-colors cursor-pointer shadow-sm">
            <?php echo isset($this->taskData['id']) ? 'Update' : 'Add'; ?>
        </button>
        <a href="<?php echo WEB_ROOT; ?>/?cancelled=1" class="bg-[#f0f4fa] border-[3px] border-[#9ca3af] text-[#6b7280] px-7 py-2 rounded-2xl font-medium text-sm transition-all hover:bg-gray-100 text-center">
            Cancel
        </a>
    </div>
</form>
