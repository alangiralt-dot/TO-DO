<?php
$tasks = $this->_data['tasks'];
if (isset($tasks)) {
    ob_start();
    echo '<main class="flex-1 flex flex-col gap-1 p-8 items-center justify-center">
		<div class="w-full max-w-6xl border-2 border-indigo-200 rounded-sm min-h-[420px]">';
    include __DIR__ . 'filterBar.phtml';
    foreach ($tasks as $task) {
    }
}
